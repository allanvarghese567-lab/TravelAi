
{
  "name": "travel-ai-platform",
  "version": "1.0.0",
  "private": true,

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "build": "npm run build --workspaces",
    "build:all": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "changed:services": "ts-node scripts/changed-services.ts"
  },

  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}